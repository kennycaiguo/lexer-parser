#include com.h

int a = v;
int c;


class Processor(int speed, string name){
	int x;
	function hello(){

		int x = 1;
	};


};

function main(int x, string y){
	int x = 3;
	x = x+1;

	if(x <= 1){
		x = 1;
	};

	if(x == 1){
		x = 1;
	} else {
		x = 2;
	};

	proc = Processor(isa = "ARM", clock_speed = 30*x);
};